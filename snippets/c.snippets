snippet using
	using namespace ${1}

snippet tern
	${1:/* condition */} ? ${2:a} : ${3:b}

snippet switch
	switch (${1:/* variable */}) {
		case ${2:/* variable case */}:
			${3}
			${4:break;}${5}
		default:
			${6}
	}${7}

snippet for
	for (${2:i} = 0; $2 < ${1:count}; $2${3:++}) {
		${4}
	}${5}
